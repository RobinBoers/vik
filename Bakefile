#!/bin/sh

use elixir
use phoenix

export AUTH_PASSWORD=pannekoek

export SCRY_ENDPOINT=http://localhost:4002
export SCRY_TOKEN=oompaloompadoompadeedo

setup() {
	super
	mix ecto.setup
}

deploy() {
	lift deploy vik
	lift deploy fog
}
